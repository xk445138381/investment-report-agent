# Copy to .env before running docker compose for production rehearsal.
# Values must be public deployment URLs, not localhost.

FRONTEND_ORIGINS=https://app.example.com
NEXT_PUBLIC_API_URL=https://api.example.com/api/v1
MONGODB_DATABASE=tradingagents
